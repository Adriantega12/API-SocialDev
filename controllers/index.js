const usersController = require('./users');
const postsController = require('./posts');
const postsController = require('./comments');

module.exports = {
  usersController,
  postsController,
  commentsController,
};
